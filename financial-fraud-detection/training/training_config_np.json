{
    "paths": { "data_dir": "/data", "output_dir": "/trained_models" },
    "models": [
        {
            "kind": "GNN_XGBoost_NP",
            "gpu": "single",
            "hyperparameters": {
                "gnn": { "hidden_channels": 32, "n_hops": 2, "layer": "SAGEConv",
                         "dropout_prob": 0.2, "batch_size": 4096, "fan_out": 8,
                         "metric": "f1", "num_epochs": 10, "weight_decay": 0.00001 },
                "xgb": { "max_depth": 3, "learning_rate": 0.1, "num_parallel_tree": 50,
                         "num_boost_round": 100, "gamma": 0.0 }
            }
        }
    ]
}
