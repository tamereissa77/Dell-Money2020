name: frontend_backend_initial_greeting
user:
  modality: text
judge:
  modality: text
  eval:
    service: ollama
    model: gemma2:9b
turns:
  - expect:
      - event: llm_started
        within_ms: 45000
      - event: response
        eval: "the reply is a brief initial greeting from Ava, the G Force Airlines flight-booking assistant"
        within_ms: 90000
