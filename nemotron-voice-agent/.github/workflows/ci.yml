# SPDX-FileCopyrightText: Copyright (c) 2024-2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: BSD-2-Clause
#
# Lightweight CI: Python lint/format (ruff), Python unit tests (pytest), and
# client lint + build (eslint, tsc, vite). No deployment and no GPU are required.

name: Nemotron Voice Agent CI

on:
  workflow_dispatch:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-lint:
    name: Python lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Ruff lint
        run: uvx ruff@0.15.6 check .

      - name: Ruff format check
        run: uvx ruff@0.15.6 format --check .

  unit-tests:
    name: Unit tests (pytest)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --dev

      - name: Run unit tests
        run: uv run pytest tests/unit -v

  pipecat-evals:
    name: Pipecat eval smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --group eval

      - name: Validate eval transport wiring
        run: PYTHONPATH=src uv run --group eval python tests/pipecat_evals/ci/smoke.py

      - name: Run service-free eval suite
        run: PYTHONPATH=src uv run --group eval python -m pipecat.evals suite tests/pipecat_evals/ci/manifest.yaml --timeout 5 --name ci

  client-lint-build:
    name: Client lint and build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install client dependencies
        run: npm --prefix client ci

      - name: Lint client
        run: npm --prefix client run lint

      - name: Build client
        run: npm --prefix client run build
