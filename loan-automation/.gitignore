# secrets — copy .env.example to .env and supply your own keys
.env
# partner-supplied model weights (large, licensed separately)
models/
weights/
*.safetensors
*.bin
**/__pycache__/
*.pyc
data/
uploads/

# HuggingFace model cache (16GB, fetched separately)
hf-cache/

# Partner-supplied sample documents. These appear to be a real individual's
# identity papers (national ID, bank statement, HR letter) — they must never be
# published. Obtain them from Efadatek directly.
shared_data/
