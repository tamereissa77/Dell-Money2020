# Qwen2.5-VL fine-tuning stack for RTX 5090 (Blackwell, sm_120, CUDA 12.8+).
#
# Install order matters:
#   1. PyTorch first (CUDA 12.8 nightly or stable when available)
#   2. transformers + peft (recent versions for Qwen2.5-VL support)
#   3. bitsandbytes (must have Blackwell support — install from source if 0.45+ wheels aren't out)
#   4. The rest
#
# See scripts/setup.ps1 for the recommended Windows install commands.

# --- Core ML stack ---
# IMPORTANT: install torch via scripts/setup.ps1 with the right CUDA channel; do not rely on this line alone.
torch>=2.5.0
torchvision>=0.20.0

transformers>=4.49.0
peft>=0.12.0
accelerate>=0.33.0
datasets>=2.20.0
bitsandbytes>=0.45.0       # 0.45 is the first release with Blackwell kernels
qwen-vl-utils>=0.0.8       # smart_resize, fetch_image, etc.
sentencepiece>=0.2.0
tokenizers>=0.20.0

# --- Eval / logging ---
tensorboard>=2.18.0
tqdm>=4.66.0
PyYAML>=6.0.2

# --- Image / OCR (only used by bulk_generator and the existing layout_analyzer) ---
Pillow>=10.4.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
easyocr>=1.7.1             # only needed if you still run the layout_analyzer

# --- Optional but recommended ---
# vllm>=0.6.3              # production serving — Blackwell support landing in 0.7+
# wandb>=0.18.0            # if you set WANDB_API_KEY
